# Rustfmt configuration for consistent formatting across environments
edition = "2021"
max_width = 100

# Consistent style
use_small_heuristics = "Default"
reorder_imports = true
reorder_modules = true